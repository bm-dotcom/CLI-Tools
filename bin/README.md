# bin

A fast, lightweight C CLI for binary file inspection, hex dumping, and statistical analysis.

## Features
- Hex + ASCII dump with offset/length
- Shannon entropy calculation
- Basic byte-frequency stats
- Zero dependencies beyond standard library