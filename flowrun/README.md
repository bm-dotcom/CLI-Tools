# flowrun

A lightweight YAML-driven CLI workflow runner for automation tasks.

## Features
- Sequential + parallel tasks
- Shell, Python snippets, and HTTP calls
- Progress bars & rich logging
- Configurable via YAML or --config