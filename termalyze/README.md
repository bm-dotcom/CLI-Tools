CLI for exploring CSV/TSV data and logs directly in the terminal.

## Features
- Instant summary statistics & column previews
- Numeric stats (mean, median, std, min/max) or categorical top-N
- Rich tables with color
- JSON export for piping into other tools
- Zero-config or `--config` support